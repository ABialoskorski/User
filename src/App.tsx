import { FC } from 'react';
import { AppRouting } from './AppRouting';

export const App: FC = () => (
  <div className='App'>
    <header>Header</header>
    <AppRouting />
    <footer>Footer</footer>
  </div>
);
